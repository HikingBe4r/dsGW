<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.groupware.persistent.mapper.NoticeMapper">

	<insert id="insertNotice" parameterType="noticeVO" statementType="PREPARED">
		insert into notice (employee_id, content, writeday, isRead)
		values (#{employeeId}, #{content}, sysdate, '0') <!-- 안 읽은게 0 --> 
	</insert>
	
	
	<select id="selectNoticeList" parameterType="string" resultType="noticeVO" statementType="PREPARED">
		SELECT 
			id,
			employee_id,
			content,
			to_char(writeday, 'YYYY/MM/DD'),
			isRead
		FROM NOTICE
		WHERE employee_id = #{value}
		ORDER BY writeday desc
	
	</select>
	
		<update id="updateNotice" parameterType="noticeVO"
		statementType="PREPARED">
		UPDATE NOTICE
		SET ISREAD = #{isread}
		WHERE ID = #{id}
	</update>

	<delete id="deleteNotice" parameterType="noticeVO"
		statementType="PREPARED">
		DELETE
		FORM NOTICE
		WHERE ID = #{id}
	</delete>
	
</mapper>